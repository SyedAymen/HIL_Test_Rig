# Copy to .env.local for local dev against a real rig, or .env.production for build.
VITE_WS_URL=ws://rig-4292.local:1880/ws/dashboard

# Set to false once Node-RED is streaming real telemetry over the socket above.
VITE_DEMO_SIMULATOR=true
